using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using FMOD;

public class AudioManager : MonoBehaviour
{
    /*
    [SerializeField] EventReference TitelscreenTheme;

    // Start is called before the first frame update
    void Start()
    {
        PlayTitelscreenTheme();
    }

    public void PlayTitelscreenTheme()
    {
        RuntimeManager.PlayOneShot(TitelscreenTheme);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
    */
}
